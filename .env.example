# SOAP Generation API Environment Variables

# Port (automatically set by Render, default is 8000 for local development)
PORT=8000

# Optional: Logging level
LOG_LEVEL=INFO

# Optional: Model cache directory
# HF_HOME=/app/models

# Optional: Database path
# DATABASE_URL=sqlite:///./data/clinic.db
